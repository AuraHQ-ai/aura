/**
 * Auto-generated OpenAPI types placeholder.
 *
 * Run `pnpm generate-api-types` with VITE_API_URL set to regenerate
 * from the live Hono OpenAPI spec at /api/dashboard/openapi.json.
 */

export interface paths {
  [path: string]: {
    [method: string]: {
      parameters?: unknown;
      requestBody?: unknown;
      responses?: unknown;
    };
  };
}

export interface components {
  schemas: Record<string, unknown>;
}
