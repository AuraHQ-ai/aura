{
  "buildCommand": "pnpm --filter aura-dashboard-v2 build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/:path(.*)", "destination": "https://aura-alpha-five.vercel.app/api/:path" },
    { "source": "/:path((?!assets/).*)", "destination": "/index.html" }
  ]
}
