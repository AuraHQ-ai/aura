---
description: How to add new tools to Aura -- the pattern for creating AI SDK tools that the LLM can call.
globs: src/tools/**/*.ts
alwaysApply: false
---

# Adding Tools to Aura

## Tool file pattern

```typescript
import { tool } from "ai";
import { z } from "zod";

export function createMyTools() {
  return {
    my_tool: tool({
      description: "Clear description of what this does and when to use it.",
      inputSchema: z.object({
        param: z.string().describe("What this parameter is"),
      }),
      execute: async ({ param }) => {
        try {
          // ... implementation
          logger.info("my_tool called", { param });
          return { ok: true, result: "..." };
        } catch (error: any) {
          logger.error("my_tool failed", { error: error.message });
          return { ok: false, error: `Failed: ${error.message}` };
        }
      },
    }),
  };
}
```

## Checklist for new tools

### Step 0 -- Do you even need a new tool?

Before writing any code, **check if the platform SDK already provides what you need**. Read the docs, search GitHub for examples. Most "tools" that went wrong in this codebase were reimplementations of existing SDK features. A new tool is only justified when:

- The LLM needs to call something that no existing tool covers
- The existing `run_command` primitive can't handle it (most things it can)
- You've verified the platform SDK doesn't already do it

Prefer **fewer, better primitives** over specialized one-shot tools. If the use case can be handled by `run_command` + a skill/rule that teaches the LLM how, that's better than a rigid hardcoded tool.

### Steps 1-9 -- If you do need a new tool

1. Create tool in `src/tools/` (new file or existing)
2. Use `inputSchema` (NOT `parameters`) -- AI SDK v6
3. Return `{ ok: true, ... }` on success, `{ ok: false, error: "..." }` on failure
4. Log with `logger.info` / `logger.error`
5. **If using an external SDK (e2b, tavily, etc.): use dynamic `import()` NEVER static `import`** -- static imports of ESM-only packages crash the entire Vercel function
6. **If the tool needs an API key, check it exists** and return a graceful error if missing (don't crash)
7. Spread into `createSlackTools` in `src/tools/slack.ts`
8. Add to the tools list in `src/personality/system-prompt.ts`
9. Add usage guidance in the system prompt
10. Document any new Slack scopes or env vars needed in README
