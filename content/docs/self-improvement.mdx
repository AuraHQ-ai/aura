---
title: "Self-Improvement"
description: "How Aura identifies gaps, files issues, and opens PRs against her own codebase."
---

# Self-Improvement

Aura can modify her own source code through a controlled feedback loop. This is what makes her capabilities compound over time.

## The Loop

```
Observe gap → Explore source → File issue → Dispatch agent → Open PR → Human review → Merge → Issue auto-closed
```

### 1. Observe

Gaps surface naturally during operation:
- A tool returns an error
- A user asks a question Aura can't answer
- Memory retrieval returns irrelevant results
- A user gives corrective feedback in conversation
- A recurring job fails repeatedly

Two automated systems also feed the loop:
- The **job supervisor** (see [Jobs](/tools/jobs)) reviews every job outcome. When it diagnoses a likely code or config bug, it opens a GitHub issue labelled `auto-supervisor-fix` automatically -- no human or Aura action required.
- The **eval funnel** (see [Eval Funnel](/eval-funnel)) scores every assistant response and tags failures with a failure class (`missing_tool`, `bad_memory`, `reasoning`, ...), giving an aggregate view of where to improve next.

### 2. Explore Source

Aura explores her own codebase using a Linux sandbox with full shell access. She can `git clone`, `grep`, read files, and run code directly. The GitHub CLI (`gh`) is pre-installed for issue and PR management.

```bash
# Example: Aura checking her own tool definitions
cd /home/user/aura
cat apps/api/src/tools/slack.ts
```

### 3. File Issue

When a gap is identified, Aura creates a GitHub issue with:
- Description of the problem
- Expected vs actual behavior
- Relevant code references
- Proposed approach (when she has one)

### 4. Dispatch Agent

Aura can dispatch a [Cursor](https://cursor.com) cloud agent to write the fix. The agent receives a detailed prompt, has access to the full codebase, and produces a PR with the changes.

Alternatively, Aura can write fixes directly using her sandbox -- cloning the repo, making changes, committing, and pushing a branch.

### 5. Open PR

The fix is committed to a feature branch and a PR is opened against `main`. The PR description references the original issue and explains the change.

### 6. Human Review

This is the critical safety constraint. **Aura cannot push to `main`.** Every change requires human approval. The team reviews the PR like any other -- checking logic, testing, and ensuring it aligns with the project's direction.

### 7. Merge Closes the Loop Automatically

A GitHub webhook (`apps/api/src/webhook/github.ts`) listens for `pull_request` events. When a PR is merged, it parses the PR body for closing keywords (`Fixes #N`, `Closes #N`, `Resolves #N`) and closes each referenced issue via the GitHub REST API -- no manual issue cleanup needed. Per-issue failures (already closed, not found, API error) are fail-soft and logged; one bad reference never blocks the rest. PR state (open/merged/closed, linked issues) is also persisted to the `github_pull_requests` table for tracking. Requires `GITHUB_WEBHOOK_SECRET` (see [Environment Variables](/deployment/environment-variables)) to verify webhook signatures -- deliveries are rejected without it.

## What Aura Has Improved

Examples of self-identified and self-fixed issues:

- **Hybrid search** — Identified that vector-only search couldn't find people by name, proposed and implemented full-text search fallback
- **Memory consolidation** — Found that decay logic wasn't running as designed
- **Response formatting** — Learned from user feedback to prefer shorter, structured responses
- **Error handling** — Caught and handled common BigQuery and Slack API error modes
- **Timezone awareness** — Built timezone-aware scheduling after sending poorly-timed messages
- **Extended thinking** — Added reasoning trace capture for better debugging
- **Conversation persistence** — Built full conversation cost tracking and trace storage

## Safety Constraints

- All code changes go through pull requests
- PRs require human approval before merging
- Self-edits to the system prompt are flagged explicitly in the PR
- PR review and merge are enforced by GitHub -- branch protection and repo permissions, not Aura's role system. Aura's roles (see [Permissions](/permissions)) gate which users can direct her to make code changes; `AURA_ADMIN_USER_IDS` is a hard override checked before the DB role
- Vercel auto-deploys from `main`, so merging = deploying

## The Compounding Effect

Self-improvement compounds because each capability unlocks the next:

1. Better memory → better context understanding
2. Better context → more accurate gap identification
3. More accurate gaps → higher quality fixes
4. Higher quality fixes → more trust from reviewers → faster merge cycles
5. Faster merges → more capabilities → loop repeats
